[pytest]
asyncio_mode = auto
testpaths = src/tests
# hard-ignore legacy dirs (spaces wreak havoc on imports)
addopts =
  --ignore=purpose_t
  --ignore=testing
  --ignore-glob="**/Extra data old testing/*"
